At node 0
cd ~/.ssh
vi authorized_keys 
sudo chown -R $USER /data
mkdir -p /data/tmp
mkdir -p /data/namenode
mkdir -p /data/datanode
git clone https://github.com/proywm/cisece578-a1.git
cd cisece578-a1
sudo apt-get update
sudo apt-get install ca-certificates curl
 sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
/install-docker.sh 
./docker-build.sh
docker swarm init --advertise-addr 10.10.1.1



docker network create -d overlay --attachable spark_net
ocker run --rm -it --name server --network spark_net alpine
docker run -d --name nn --network spark_net -p 9870:9870 -v /data:/data -v ./src:/src -v /proj/uwmadison744-s25-PG0/wikidata/enwiki-pages-articles:/enwiki-pages-articles hdfs-namenode